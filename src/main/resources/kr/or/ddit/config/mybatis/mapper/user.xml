<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="user">
	<select id="getUserList" resultType="userVo">   <!--  mybatis는 별칭 설정은 mybatis-config에서 설정한다. -->
		select * from users
	</select>
	
	<select id="getUser" parameterType="String" resultType="userVo">
		select * from users
		where userId = #{userId}
	</select>
	
	<select id="getUserOnlyHalf" resultType="userVo">   <!--  mybatis는 별칭 설정은 mybatis-config에서 설정한다. -->
		select userId, USERNM, ALIAS, REG_DT ,rownum
		from users
		where rownum &lt;= 50
	</select>
</mapper>